{
  "build": {
    "command": "node -e \"console.log('ImmoFind ready')\""
  },
  "deploy": {
    "startCommand": "npx prisma generate && npm start",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
